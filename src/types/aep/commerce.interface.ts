import { ProductListAdds } from "./addToCart.interface";

/** Product returns, warranty registration, and shopping cart/order process. */
export interface Commerce {
    productListAdds?: ProductListAdds;
}
